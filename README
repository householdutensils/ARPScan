ARPScan.pl is a script that takes a single argument of a local interface. It then scans the directly connected subnet for hosts that respond to ARP Requests. 

At the moment, the interface and mac address are hard coded as there are some bugs with the function to parse the int info
